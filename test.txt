ORIGIN IS (200,200);
ROT IS 0;
SCALE IS (1,1);
FOR T FROM 0 TO 2*PI STEP PI/50 DRAW (100*cos(T), 100*sin(T));
ORIGIN IS (50*sqrt(2), 50*sqrt(2));
ROT IS PI/3;
FOR T FROM 0 TO 100*sqrt(2) STEP 1 DRAW (T,T);